<h1>
  <%= @deploy.summary_for_email %>
</h1>

<p>The detailed log can be viewed <%= link_to 'here', project_deploy_url(@project, @deploy) %></p>

<h2>Pull Requests</h2>
<%= render 'changeset/pull_requests', pull_requests: @changeset.pull_requests, type: "deploy" %>

<h2>Risks</h2>
<%= render 'changeset/risks', pull_requests: @changeset.risky_pull_requests, type: "deploy" %>

<h2>Commits</h2>
<%= render 'changeset/commits', changeset: @changeset %>

<h3>Pull Requests</h3>
<%= render 'changeset/pulls', changeset: @changeset %>
